import 'package:flutter/material.dart';

class AddLocationScreen extends StatelessWidget {
  static const String route = '/location/new';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
